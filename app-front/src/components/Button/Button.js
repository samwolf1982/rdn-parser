import React from 'react';
import PropTypes from 'prop-types';

const Button = props => {
    return (
        <div>
           <button>click me!</button>
        </div>
    );
};

Button.propTypes = {

};

export default Button;